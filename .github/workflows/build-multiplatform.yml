name: Build multiplatform

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release git tag
        type: string
        required: true
  push:
  pull_request:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
        - name: Windows
          os: windows-2022
          artifactsPath: ./src/dist/*.exe
        - name: Linux
          os: ubuntu-22.04
          artifactsPath: ./src/dist/*.AppImage
        # MacOS Intel is gone: macos-13 was the last x86_64 runner GitHub
        # offered, and jobs targeting it now sit in the queue forever.
        - name: MacOS Silicon
          os: macos-latest
          artifactsPath: ./src/dist/*.zip
        - name: Linux ARM64
          os: ubuntu-22.04-arm
          artifactsPath: ./src/dist/*.AppImage
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.11.3
      uses: actions/setup-python@v3
      with:
        python-version: "3.11.3"
      if: ${{ !startsWith(matrix.name, 'MacOS') }}
    # The setup-python interpreter ships a Tk whose widgets stop responding to
    # clicks in the frozen .app. Homebrew's python-tk builds a working one.
    - name: Set up Homebrew Python with Tk (MacOS)
      run: |
        brew install python@3.11 python-tk@3.11
        echo "/opt/homebrew/opt/python@3.11/libexec/bin" >> $GITHUB_PATH
      if: ${{ startsWith(matrix.name, 'MacOS') }}
    - name: Install dependencies
      run: |
        cd ./src/
        python -m pip install --upgrade pip
        python dependencies.py
    - name: Build ${{ matrix.name }}
      run: |
        cd ./src/
        python ./compile_onedir.py
      if: ${{ endsWith(matrix.name, 'dir') || startsWith(matrix.name, 'MacOS') }}
    - name: Build ${{ matrix.name }}
      run: |
        cd ./src/
        python ./compile.py
      if: ${{ !endsWith(matrix.name, 'dir') && !startsWith(matrix.name, 'MacOS') && !endsWith(matrix.name, 'nuitka') }}
    - name: Upload artifacts
      uses: actions/upload-artifact@v4.6.0
      with:
        name: artifacts-${{ matrix.name }}
        path: ./src/dist
        retention-days: 7
    - name: Publish release
      if: ${{ github.event_name == 'workflow_dispatch' }}
      uses: ncipollo/release-action@v1
      with:
        artifacts: ${{ matrix.artifactsPath }}
        tag: ${{ inputs.tag }}
        name: NX Optimizer ${{ inputs.tag }}
        allowUpdates: true
        artifactErrorsFailBuild: false
        prerelease: true
        token: ${{ secrets.GITHUB_TOKEN }}
